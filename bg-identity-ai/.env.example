# =================================================================
# EXAMPLE ENVIRONMENT CONFIGURATION
# bg-threat-ai Console-First Threat Detection Platform
# =================================================================

# Copy this file to .env for development or .env.production for production
# Replace all placeholder values with your actual configuration

# Environment Configuration
NODE_ENV=development
LOG_LEVEL=info
PORT=3002

# =================================================================
# SECURITY CONFIGURATION (REQUIRED)
# =================================================================

# JWT Authentication - MUST BE CHANGED FOR PRODUCTION
JWT_SECRET=your-256-bit-secret-key-here-change-for-production
JWT_EXPIRES_IN=24h

# API Security
BG_WEB_API_KEY=your-api-key-here
BG_WEB_API_URL=http://localhost:3000

# =================================================================
# REDIS CONFIGURATION (REQUIRED)
# =================================================================

# Basic Redis Connection
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_URL=redis://localhost:6379/0

# Redis Performance (Optional)
REDIS_MAX_RETRIES=3
REDIS_COMMAND_TIMEOUT=5000
REDIS_CONNECT_TIMEOUT=10000

# =================================================================
# OPTIONAL CONFIGURATIONS
# =================================================================

# Performance Settings
API_RATE_LIMIT_MAX_REQUESTS=1000
API_TIMEOUT=30000
MAX_MEMORY_USAGE_MB=512

# Cache TTL Settings (seconds)
CACHE_TTL_DEFAULT=300
CACHE_TTL_THREATS=300
CACHE_TTL_BEHAVIOR=3600
CACHE_TTL_INTELLIGENCE=14400

# WebSocket Configuration
WEBSOCKET_ENABLED=true
WEBSOCKET_MAX_CONNECTIONS=1000

# Monitoring
HEALTH_CHECK_INTERVAL=30000
METRICS_COLLECTION_ENABLED=true
PERFORMANCE_TRACKING_ENABLED=true

# Logging
LOG_FORMAT=json
LOG_CORRELATION_ID_HEADER=x-correlation-id

# =================================================================
# EXTERNAL SERVICES (OPTIONAL)
# =================================================================

# Monitoring and Alerting
SENTRY_DSN=
SLACK_WEBHOOK_URL=
DISCORD_WEBHOOK_URL=

# Threat Intelligence APIs
THREAT_INTEL_API_KEY=
THREAT_INTEL_API_URL=

# Email Configuration
EMAIL_SMTP_HOST=
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=
EMAIL_SMTP_PASSWORD=

# =================================================================
# PRODUCTION-SPECIFIC (For .env.production)
# =================================================================

# HTTPS Settings
HTTPS_ENABLED=false
SSL_CERT_PATH=/etc/ssl/certs/cert.pem
SSL_KEY_PATH=/etc/ssl/private/key.pem

# Database (if applicable)
DATABASE_URL=
DB_SSL_ENABLED=true
DB_CONNECTION_POOL_MAX=10

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Security Headers
HELMET_CSP_ENABLED=true
HELMET_HSTS_MAX_AGE=31536000

# =================================================================
# SETUP INSTRUCTIONS
# =================================================================

# 1. Copy this file:
#    cp .env.example .env
#
# 2. Required changes:
#    - Set JWT_SECRET to a secure 256-bit key
#    - Configure Redis connection (REDIS_HOST, REDIS_PORT, etc.)
#    - Set BG_WEB_API_KEY for service communication
#
# 3. Optional configurations:
#    - External monitoring services (Sentry, Slack)
#    - Threat intelligence API keys
#    - Email SMTP settings
#
# 4. For production deployment:
#    - Copy to .env.production
#    - Replace all placeholder values
#    - Enable HTTPS and security headers
#    - Configure external services and monitoring
#
# 5. Security best practices:
#    - Never commit .env files to version control
#    - Use external secret management for production
#    - Regularly rotate JWT secrets and API keys
#    - Monitor access logs for anomalies